version: '3.1'

services:

  mongo:
    container_name: stockanalyzer-mongodb
    image: mongo
    restart: always
    ports:
      - 27018:27017
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example
    volumes:
      - ./.docker-volumes/mongodb:/data/dbŌ